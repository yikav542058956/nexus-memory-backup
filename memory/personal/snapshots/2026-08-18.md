# Session Snapshot 2026-08-18

> Append-only digest of conversations whose extraction informed today's memory build.

## Summary
- Redesigned AI Provider admin section (Add Provider button → select provider → key+model input → key list with Use-as-active/Remove).
- Set OpenRouter as active provider with free model google/gemma-4-31b-it:free.
- Implemented multi-provider multi-key multi-model automatic fallback chain (OpenRouter → Groq → Gemini).
- Customer name auto-capture from WhatsApp pushName; owner number auto-detect.
- Fixed over-aggressive escalation detector (removed broad 'batao' pattern).
- Fixed slow reply (~1 min → ~1 sec) via 15s timeout and speed-ordered free models.
- Added purchase-options guidance (online website vs local address + owner number).
- Implemented number-based text menu (1=Browse, 2=Language, 3=Owner) after discovering Baileys blocks interactive buttons on non-Business API accounts.
- Enhanced owner notification with customer name + number + wa.me link.
